{
  "name": "study-plan",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p packages/shared",
    "lint": "biome check .",
    "format": "biome check --write .",
    "presets:validate": "bun packages/shared/scripts/validate-presets.ts",
    "presets:schema": "bun packages/shared/scripts/generate-preset-schema.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^24.0.0",
    "fast-check": "^4.0.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
